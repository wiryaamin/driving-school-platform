/**
 * Auth store — thin wrapper over SessionStore for auth-specific operations.
 * Imports from session.store and adds auth-specific selectors.
 */
export { useSessionStore } from '../store/session.store.js';
